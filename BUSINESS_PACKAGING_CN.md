# 企业级包装方案

## 一句话定位

把这个项目包装成：**面向金融、投研、风控与集团管理场景的多实体财报智能分析平台**，而不是“比赛用的一司一库问答脚本”。

它的核心卖点不是聊天，而是：

- 对长篇财报和监管文件做高质量解析
- 基于证据做可追溯问答
- 支持跨公司、跨报告、跨指标比较
- 支持会话记忆与研究过程沉淀
- 能从离线批处理自然过渡到在线服务化

## 建议你对外怎么讲这个项目

### 产品名称建议

- 多实体财报智能分析平台
- Financial Document Intelligence Copilot
- 企业级监管文件与财报问答中台
- 投研与风控文档情报平台

### 典型客户对象

- 券商研究所
- 公募/私募投研团队
- 银行对公授信和风险管理团队
- 集团战略投资部
- CFO 办公室、IR、审计、合规团队
- 专注于 ESG、供应链、并购尽调的数据团队

### 典型业务场景

1. 横向对比多个公司的财务指标、资本结构、分红政策、管理层变化
2. 针对单家公司连续多年年报做纵向趋势追踪
3. 对投研问题做证据定位，直接返回出处页码
4. 对表格密集型财报做结构化抽取，提高指标问答命中率
5. 在会话中沉淀“已知事实”，减少重复检索，支持连续比较和追问

## 你现在这套技术，怎么包装得更像真实企业系统

## 1. 不要把它讲成“一司一库”

更合适的表述是：**按文档/实体/报告期进行知识分片的检索架构**。

这和真实企业系统并不冲突，反而很常见，因为它天然带来：

- 数据隔离：不同公司、不同租户、不同报告期边界清晰
- 增量更新：新年报到了，只重建对应分片即可
- 可解释性：答案能稳定回溯到具体报告和具体页码
- 权限治理：更容易做实体级、客户级、部门级授权
- 成本控制：先路由到候选文档分片，再做检索，不必全库粗暴召回

所以建议你把当前检索单元理解为：

- `report shard`
- `entity-scoped knowledge shard`
- `filing-level index`

而不是简单说“每个公司单独一个库”。

## 2. 把当前系统包装成三层架构

### 第一层：文档理解与知识生产层

对应现有模块：

- `src/pdf_parsing.py`
- `src/tables_serialization.py`
- `src/parsed_reports_merging.py`
- `src/text_splitter.py`
- `src/ingestion.py`

业务话术：

> 负责把原始 PDF 财报、监管文件和附表转化为可检索、可引用、可追踪的知识资产。

### 第二层：检索与推理层

对应现有模块：

- `src/retrieval.py`
- `src/reranking.py`
- `src/questions_processing.py`
- `src/prompts.py`
- `src/api_requests.py`

业务话术：

> 负责根据问题自动识别目标实体、召回相关证据、重排上下文，并生成带出处的结构化结论。

### 第三层：任务编排与交互层

对应现有模块：

- `api.py`
- `worker.py`
- `app.py`
- `api_single.py`
- `app_single.py`

业务话术：

> 负责承接用户会话、管理异步任务、沉淀研究记忆，并支持多轮问答和跨实体比较。

## 3. 重新定义你的几个核心亮点

### 亮点一：不是普通 PDF QA，而是“财报级文档理解”

你的优势不是只有向量检索，而是前面做了很重的文档预处理：

- OCR
- 版面解析
- 表格提取
- 页级文本重构
- markdown 化清洗

这很适合包装成：

> 专门面向财报、年报、监管文件等复杂长文档的文档理解引擎。

### 亮点二：不是只会检索，而是“证据可追溯的分析回答”

你当前输出里已经有：

- `value`
- `reasoning_process`
- `references`

这非常适合包装成：

> 每个回答均附带证据页码，满足投研、审计、风控对可解释性的要求。

### 亮点三：不是只能单文档问答，而是“多实体比较分析”

`QuestionsProcessor` 已支持 comparative 问题，`worker.py` 又增加了基于 memory 的比较短路能力。

你可以把它包装成：

> 支持跨公司指标对比、同一主题对比分析，以及多轮连续研究。

### 亮点四：不是静态离线脚本，而是“可服务化部署”

你现在已有：

- FastAPI
- Redis
- Worker
- Streamlit

所以完全可以包装成：

> 既支持离线批量处理，也支持在线问答服务与异步任务编排。

## 4. 建议你对“真实业务”的叙事模板

你可以直接用下面这段来介绍项目：

> 本项目是一个面向企业财报、监管文件和长文本披露材料的智能分析平台。系统通过 PDF 解析、表格结构化、分片检索、LLM 重排与结构化回答等能力，将原本难以消费的长篇财务文件转化为可问答、可比较、可追溯的企业知识资产。平台既支持离线批量构建知识库，也支持在线会话式查询、跨公司比较和研究记忆沉淀，适用于投研、风控、审计、合规、IR 与管理层分析等场景。

## 5. 最小改动下，如何把当前实现说得更合理

你不用大改代码，可以直接这样解释现状：

### 当前实现

- 每份报告构建独立向量索引
- 通过实体识别或问题路由，先锁定候选公司/文档
- 再在候选分片内进行检索和重排
- 输出结构化答案与证据引用

### 对外表述

> 当前版本采用文档分片式索引架构，以保证解释性、更新效率和实体隔离能力。在后续演进中，可进一步扩展为统一元数据注册表 + 分片路由 + 混合召回的企业级多文档检索体系。

这句话很关键，因为它把“临时实现”翻译成了“可演进架构”。

## 6. 如果你要再往前走半步，最小演进建议

下面这些都属于小改，不会破坏你现有技术主干：

1. 增加一个“文档注册表”概念
   - 记录 `company / year / document_type / currency / industry / sha1`
   - 让系统看起来更像企业知识中台

2. 把检索前的实体识别包装成“路由层”
   - 先筛文档分片，再做向量检索
   - 这比“全库检索”更像真实业务系统

3. 把 `worker.py` 中的 memory 讲成“研究会话记忆”
   - 支持 analyst workflow
   - 支持跨轮比较和连续追问

4. 把 `app.py` 包装成“分析工作台”而不只是聊天界面
   - 左侧是结构化记忆
   - 中间是问答主线程
   - 底层可返回推理与引用

## 7. 你可以怎么回答别人对“一司一库”的质疑

可以直接这样说：

> 现阶段不是按“全量大库统一混检”的思路实现，而是采用按报告或实体进行知识分片的方式。这样做的原因是财报类场景对可解释性、版本边界、增量更新和权限隔离要求更高。系统先通过路由确定候选文档范围，再在局部分片内检索和重排。这种方式在企业内部知识系统、监管文件检索和多租户场景里是合理且常见的。

## 8. 一句更像产品经理/架构师的总结

> 这不是一个单纯的 RAG Demo，而是一个围绕企业财务文档构建的“文档理解 + 检索推理 + 服务编排”三层智能分析平台。当前版本已经具备企业级场景最关键的几个特征：复杂文档解析、证据可追溯、跨实体比较、会话记忆和服务化接口；后续只需围绕元数据治理、统一路由和权限控制做增强，就可以自然升级为更完整的企业知识中台方案。
